# SmartTutor AI - Required Python Packages
# Install all of these using: pip install -r requirements.txt

# Web app framework - used to build the entire user interface
streamlit

# Official Google Gemini SDK - used to talk to the Gemini AI model
google-genai

# Used to read text out of uploaded PDF files
PyPDF2

# Used to load our secret API key from the .env file
python-dotenv
